---
title: "Deposit"
description: "Deposit items from Steam inventory to Mannco.store"
---

## Get deposit information

```
GET /deposit/{game}
```

**Permission:** Connected + API

<ParamField path="game" type="integer" required>
  Game ID (`440` for TF2, `730` for CS2, `570` for Dota 2, `252490` for Rust)
</ParamField>

Returns pricing information and available items from the user's Steam inventory for the specified game.

### Response

```json
{
  "status": "success",
  "data": {
    "informations": [
      {
        "assetid": "123456789",
        "market_hash_name": "Unusual Burning Flames Team Captain",
        "unusual": "Burning Flames",
        "craft": "",
        "spell": "",
        "sheen": "",
        "killstreaker": "",
        "image": "https://...",
        "class": "Hat",
        "quality": "Unusual",
        "depositkey": "12345_0",
        "price": 150000,
        "disabled": ""
      }
    ]
  }
}
```

### Item Fields

| Field | Type | Description |
|-------|------|-------------|
| `assetid` | string | Steam asset ID |
| `market_hash_name` | string | Steam Market name |
| `unusual` | string | Unusual effect name (if applicable) |
| `craft` | string | "uncraftable " if not craftable, empty otherwise |
| `spell` | string | Spell names separated by semicolons |
| `sheen` | string | Sheen effect |
| `killstreaker` | string | Killstreaker effect |
| `price` | integer | Mannco.store buy price in cents |
| `disabled` | string | Reason if item is not accepted (e.g., "blacklist", "highstock") |
| `depositkey` | string | Unique identifier for the deposit item |
| `image` | string | Item image URL |

<Info>
  Only items that are tradable and have a valid price will have a non-empty `depositkey`. Items with `disabled` field set cannot be deposited.
</Info>

---

## Get bot list

```
GET /deposit/botList/{game}
```

**Permission:** API

<ParamField path="game" type="integer" required>
  Game ID
</ParamField>

Returns a list of available trade bots for the specified game, including their status and capacity.

### Response

```json
{
  "status": "success",
  "data": [
    {
      "steamId": "76561198000000000",
      "name": "Mannco Bot #1",
      "online": true,
      "capacity": 850,
      "currentItems": 645,
      "tradeUrl": "https://steamcommunity.com/tradeoffer/new/?partner=..."
    }
  ]
}
```

---

## Get trade status

```
GET /deposit/tradeStatus/{tradeid}
```

**Permission:** Connected + API

<ParamField path="tradeid" type="string" required>
  Steam trade offer ID
</ParamField>

Checks the current status of a deposit trade offer.

### Response

```json
{
  "status": "success",
  "data": {
    "tradeId": "987654321",
    "status": 3,
    "statusText": "Completed",
    "items": [
      {
        "assetId": "123456789",
        "name": "Unusual Team Captain",
        "price": 150000
      }
    ],
    "totalPrice": 150000,
    "timestamp": "1706745600",
    "completedAt": "1706749200"
  }
}
```

### Trade Status Values

| Status | Description |
|--------|-------------|
| `0` | Pending - waiting for acceptance |
| `3` | Completed - items credited to account |
| `-1` | Failed - trade was declined or expired |

---

## Set price for upcoming trade

```
POST /deposit/setPriceForUpcomingTrade
```

**Permission:** Connected + API

Pre-sets custom prices for items before creating a deposit trade. Useful for negotiating prices or handling items with dynamic pricing.

### Body Parameters

<ParamField body="items" type="array" required>
  Array of items with assetId and custom price
</ParamField>

### Request Example

```json
{
  "items": [
    {
      "assetId": "123456789",
      "price": 145000
    },
    {
      "assetId": "987654321",
      "price": 85000
    }
  ]
}
```

### Response

```json Success
{
  "status": "success",
  "data": {
    "pricesSet": 2,
    "expiresAt": "1706749200",
    "message": "Prices set for upcoming trade. Create trade within 10 minutes."
  }
}
```

<Warning>
  Custom prices expire after 10 minutes. You must initiate the deposit trade within this timeframe.
</Warning>

<Info>
  This endpoint is typically used in conjunction with a manual price negotiation process. Most users will use the standard pricing from `GET /deposit/{game}`.
</Info>
